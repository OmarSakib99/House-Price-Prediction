[project]
name = "house-price-prediction"
version = "0.1.0"
description = "Flask app for house price prediction"
requires-python = ">=3.9"
dependencies = [
    "Flask",
    "scikit-learn==1.6.1",
    "numpy",
    "gunicorn",
]
